# 待确认文档（需求确认）

- 需求编号: `SITE-001`
- 版本号: `v1`
- 需求编号来源: 自动生成（无既有编号体系；取 `SITE` + 3 位序号）
- 日期: 2026-08-19

## 1. 需求背景

在 new-api 开源项目上构建公司（iFAi · 皋如信息科技）的官网首页。输入包含：

- `docs/参考资源/iFAi 业务简介_2608C.pptx` — 公司业务/产品简介（首页设计信息参考）；
- `docs/参考资源/首页 HTML 的参考index.html`、`首页 HTML 的参考preview.html` — 两版首页 demo（结构与功能参考）；
- `docs/参考资源/intelligent-warehouse-architecture.html` — 数仓业务参考；
- 定价参考图 — 明确为「后台/控制台」配置口径，不进入官网首页范围。

## 2. 本次迭代范围

首页关键信息规划（用户给定）：

1. **Every Token Counts** — 让每次 Token 调用都可解释，让每份账单都可审计；
2. **从一次接入，到一条可以解释的经营链路** — 三层次业务结构图（Token 接入与成本优化 / Enterprise Brain / FDE），突出 EB；
3. **每一笔调用，都有回到业务现场的路径** — 摘要留存 90 天，兼顾优化、审计、数据安全；
4. **数仓业务价值**（酌情加入）— 痛点应对与解决；
5. **CTA**：带着一个真实场景，和 FDE 一起把它落地（FDE 预约表单）。

落点：`/`（无后台自定义首页内容时渲染的默认首页），保留后台「自定义首页内容」（URL/HTML/Markdown）优先级不变。

## 3. 当前系统真实现状

- 页面现状：`web/src/routes/index.tsx` → `features/home/index.tsx`。默认渲染 new-api 产品营销页（Hero/Stats/Features/HowItWorks/CTA），或渲染后台配置的自定义首页内容。
- 接口现状：`GET /api/home_page_content` 返回自定义首页内容；FDE 预约无后端接口。
- 数据现状：无数据库改动。

## 4. 需求描述与现状不一致项

1. 需求：官网首页在 `/` 呈现。
   实际现状：`/` 默认渲染 new-api 产品营销页。
   差异说明：新增 `features/company-site` 作为新的默认回退内容；旧默认区块组件文件与 i18n 键保留在代码库中，未被删除。
   影响：仅 `features/home/index.tsx` 回退分支。

2. 需求：FDE 预约应可落地。
   实际现状：无后端预约接口。
   差异说明：前端提供 `VITE_FDE_FORM_ENDPOINT` 环境变量接入任意表单后端/Webhook；未配置时按参考 demo 行为模拟提交（可完整演练加载/成功态）。生产接真后端时仅需配置该变量。
   影响：不涉及后端与数据库改动。

3. 需求：参考材料含「定价」图。
   实际现状：该图描述控制台对最终用户体现输入/输出/缓存命中价格与官网牌价一致的**后台配置口径**。
   差异说明：属于控制台/后台配置范畴，不属于官网首页内容，本轮不实现。

## 5. 潜在影响点

- 菜单与路由：`/` 回退内容变化；`/pricing`、`/sign-in`、`/dashboard` 等既有路由不受影响。
- 权限：无（公开页；登录态仅切换「进入控制台」目标 `/dashboard`）。
- 状态流转：FDE 表单新增校验/提交中/成功/失败四态。
- 报表统计：无。
- 历史数据兼容：无数据库/接口变更。
- i18n：新增 ~138 个键，覆盖 en/zh/zh-TW/fr/ru/ja/vi；`Enterprise Brain` 为品牌词，各语言保留原文。
- 受保护信息：new-api 项目归属（页脚「New API」署名与版权头）全部保留，未删除、未替换。

## 6. 待用户确认问题

无高风险歧义项。用户已在需求中明确：

- demo HTML「效果参考 并非定案 只是结构和功能参考」→ 复现模式确认为 `reference-only`；
- 数仓板块为「酌情」→ 已加入但保持克制（痛点三断裂 + 分层数仓/开发链/决策引擎示意 + 价值点）；
- 定价材料仅作后台配置参考 → 不进入首页。

## 7. UI 参考复现确认

- 是否提供 UI HTML、截图、设计稿、可运行原型或参考页面：是（两版 demo HTML）。
- 参考事实源与依赖资源：`docs/参考资源/首页 HTML 的参考index.html`、`首页 HTML 的参考preview.html`（含内联 CSS/JS；LOGO 图片资产缺失，以系统配置 Logo + 品牌点阵占位）。
- 复现模式：**`reference-only`**（用户原话：效果参考、并非定案、只是结构和功能参考）。
- 页面与路由范围：`/`（默认首页回退）。
- 目标浏览器、视口、缩放、主题和语言：Chrome；1440×900、390×844；100% 缩放；light/dark；zh/en。
- 关键交互与状态：导航锚点滚动、移动端汉堡菜单、FDE 表单校验错误/提交中/成功/失败、明暗主题切换、语言切换。
- 字体、图片、图标、Logo 等资产可用性：参考页面的 iFAi Logo 图片未提供 → 使用 `useSystemConfig` 的部署 Logo，缺失时使用四色点阵占位。
- 已确认允许偏差：不要求像素级复现；版式、间距、颜色、字体族采用 new-api 现有设计令牌（Tailwind v4 token、`landing-animate-*` 动画、Button/Footer 组件）；审计控制台示意保留深色面板风格。

# 需求确认结论

- 需求编号：`SITE-001`
- 版本号：`v3`
- 编号来源：延续现有 SITE-001 官网迭代

## 本轮范围

1. 首页明确展示 Token 供给、Enterprise Brain、FDE 三大业务。
2. Enterprise Brain 说明快速把客户业务数据建设为 AI 可检索、可治理、可用于决策的数据仓库。
3. 首页恢复项目原有顶部导航结构，并让系统通知入口展示参考图中的定价模式。
4. 首页底部与首页/控制台左上角加入 `docs/参考资源` 中的 iFAi Logo 和公司名称。
5. FDE 预约改为调用本站公开 API，并持久化到主数据库。

## 已确认口径

- UI 参考模式：沿用 v1 的 `reference-only`；不要求逐像素复刻参考 HTML。
- 公司品牌：iFAi；公司主体：皋如信息科技有限公司。
- 定价：`<7 万 RMB/月`、`7 万～70 万 RMB/月`、`>70 万 RMB/月` 三档；DeepSeek 建议官网直连。
- FDE：Forward Deployed Engineer，帮助客户把具体业务难题通过 AI 快速落地。
- New API/QuantumNous 项目身份受仓库政策保护，继续保留项目署名，不作为本轮删除对象。

## 无阻塞项

- 导航“最开始的那种”可由 SITE-001 之前的 `PublicHeader` 和动态导航配置确定。
- 数据库沿用当前主库和 GORM 自动迁移，无需指定单一数据库类型。

## 发布范围调整（2026-08-21）

- 用户补充要求不并排展示两个品牌，优先使用既有系统设置中的 `SystemName`/`Logo`。
- 仓库治理规则禁止替换受保护项目品牌，因此本次发布不修改默认 Logo、favicon、名称，也不写入数据库品牌配置。
- 最终代码只保留单一系统配置品牌机制；圆形 iFAi PNG 作为可选静态资源发布，不自动启用。
